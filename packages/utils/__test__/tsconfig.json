{
  // 继承主配置的所有严格规则和路径映射
  "extends": "../tsconfig.json",
  // 覆盖或添加测试相关的配置
  "compilerOptions": {
    // 测试环境可能需要不同的模块系统或目标环境
    "types": ["node", "vitest"] // 如果需要，添加测试框架的类型
  },
  // **关键**：明确包含测试文件
  "include": ["./**/*.ts"]
  // exclude 通常无需重复，已从主配置继承
}
